model File {
  id          String    @id @default(cuid())
  title       String
  description String
  versions    Version[]
  project     Project?  @relation(fields: [projectId], references: [id])
  projectId   String?
}
