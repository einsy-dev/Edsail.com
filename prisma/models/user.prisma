enum Role {
  USER
  ADMIN
}

enum Status {
  PENDING
  APPROVED
  BAN
}

model User {
  id       String @id @default(cuid())
  name     String
  email    String
  password String
  role     Role   @default(USER)
  status   Status @default(PENDING)
}
